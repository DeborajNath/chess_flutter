name: chess_flutter
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  bishop: ^1.4.4
  flutter:
    sdk: flutter
  gap: ^3.0.1
  provider: ^6.1.2
  square_bishop: ^0.3.0
  squares: ^1.2.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
